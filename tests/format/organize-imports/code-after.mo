// The whole file is an import section followed by declarations; the pass must not touch anything
// past the last import. Extra blank lines *within* the section collapse.
import Array "mo:base/Array";


let x = 5;
let y = 10;
