// Fields sort by name with the alias as tiebreak, so the aliased and bare entries interleave
// rather than separating by form.
import { zulu = z; alpha = a; filter; fold = foldFn; reduce } "mo:base/Array";

actor {};
