// Sorted, grouped by path prefix with a blank line between groups, one blank line before the
// declaration. The `actor` keeps this a whole program rather than an import-only file.
import Text "mo:base/Text";
import Utils "./utils";
import Core "canister:core";
import IC "ic:aaaaa-aa";
import Array "mo:base/Array";

actor {};
